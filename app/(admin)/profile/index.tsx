// app/(admin)/profile/index.tsx
import React from 'react';
import ProfilAdmin from '@/pages/ProfilAdmin';

/**
 * Renders the admin profile page by returning the ProfilAdmin component.
 *
 * @returns The JSX element for the admin profile page.
 */
export default function ProfilAdminPage() {
    return <ProfilAdmin />;
}