// app/(main)/search/index.tsx
import { View, Text } from 'react-native';

/**
 * Screen component that renders a simple "Search" label.
 *
 * @returns The root React element for the Search screen.
 */
export default function SearchScreen() {
  return (
    <View>
      <Text>Search</Text>
    </View>
  );
}