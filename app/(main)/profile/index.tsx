// app/(main)/profile/index.tsx
import { View, Text } from 'react-native';

/**
 * Renders the Profile screen showing a "Profile" label.
 *
 * @returns The React element representing the Profile screen.
 */
export default function ProfileScreen() {
  return (
    <View>
      <Text>Profile</Text>
    </View>
  );
}