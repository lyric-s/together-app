import Notifications from '@/pages/Notifications';
/**
 * Renders the Notifications page for the settings route.
 *
 * @returns A React element that renders the Notifications page.
 */
export default function NotificationsRoute() { return <Notifications />; }