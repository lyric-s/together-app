import { StyleSheet } from "react-native";

export default StyleSheet.create({
    fallback: {
        height: 160,
        borderRadius: 10,
        opacity: 0.15,
        backgroundColor: "#000",
    },
});
